---
phase: 07-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: false
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
user_setup:
  - service: vercel
    why: "Host production deployment of the Next.js app (DEP-01)"
    account_setup:
      - "Create Vercel account at https://vercel.com/signup (skip if user already has one)"
      - "Authorize Vercel to access the GitHub account hosting computer-recomendator"
    dashboard_config:
      - task: "Import GitHub repo via Add New Project -> Import Git Repository"
        location: "https://vercel.com/new"
      - task: "Select Next.js framework preset (auto-detected)"
        location: "Import screen"
      - task: "Add environment variables BEFORE clicking Deploy"
        location: "Import screen -> Environment Variables section"
  - service: supabase
    why: "Connect production deployment to existing Supabase project (DEP-02)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "User's local .env.local file OR Supabase Dashboard -> Project Settings -> API -> Project URL"
        scope: "Vercel Production (and Preview, per D-02 — same project for all envs)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "User's local .env.local file OR Supabase Dashboard -> Project Settings -> API -> anon public key"
        scope: "Vercel Production (and Preview, per D-02)"

must_haves:
  truths:
    - "Anyone visiting the Vercel production URL sees the fully functional app without authentication or local setup"
    - "Completing the quiz on the production URL returns real laptop recommendations from Supabase"
    - "The production build config object does not contain allowedDevOrigins"
    - "Local network dev (npm run dev on 192.168.x.x) still works after the config change"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js config as a phase function that only includes allowedDevOrigins during PHASE_DEVELOPMENT_SERVER"
      contains: "PHASE_DEVELOPMENT_SERVER"
    - path: "Vercel project (external, not a file)"
      provides: "Production deployment reachable at https://<project>.vercel.app"
  key_links:
    - from: "next.config.ts"
      to: "next/constants"
      via: "import { PHASE_DEVELOPMENT_SERVER } from \"next/constants\""
      pattern: "PHASE_DEVELOPMENT_SERVER"
    - from: "Vercel runtime"
      to: "Supabase"
      via: "NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY env vars in Vercel Production scope"
      pattern: "NEXT_PUBLIC_SUPABASE_"
    - from: "src/lib/supabase.ts"
      to: "process.env.NEXT_PUBLIC_SUPABASE_URL"
      via: "createClient(supabaseUrl, supabaseAnonKey)"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE_"
---

<objective>
Deploy the computer-recomendator app to a public Vercel production URL with working Supabase connectivity, and remove the dev-only `allowedDevOrigins` setting from the production config per D-01.

Purpose: Closes milestone v1.1 Phase 7 — makes the app publicly reachable so subsequent phases (SEO, Feedback, Avatars, Mobile) can be validated against a live URL.
Output: A live Vercel production URL, a cleaned-up `next.config.ts` committed to `main`, and verified Supabase data flow in production.
</objective>

<execution_context>
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/07-deploy/07-RESEARCH.md
@.planning/phases/07-deploy/07-VALIDATION.md

<interfaces>
<!-- Key files the executor must understand. Extracted from codebase. -->

Current next.config.ts (the ONLY file we're modifying):
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["192.168.100.14", "192.168.1.67", "192.168.1.74"],
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "orxstqqcsxatxaprqyvq.supabase.co", pathname: "/storage/v1/object/public/**" },
      { protocol: "https", hostname: "images.unsplash.com" },
      { protocol: "https", hostname: "http2.mlstatic.com" },
      { protocol: "https", hostname: "m.media-amazon.com" },
    ],
  },
};

export default nextConfig;
```

Target shape after edit (from RESEARCH.md Pattern 1, verified against Next.js 16.2 docs):
```typescript
import type { NextConfig } from "next";
import { PHASE_DEVELOPMENT_SERVER } from "next/constants";

export default function config(phase: string): NextConfig {
  return {
    ...(phase === PHASE_DEVELOPMENT_SERVER && {
      allowedDevOrigins: ["192.168.100.14", "192.168.1.67", "192.168.1.74"],
    }),
    images: {
      remotePatterns: [
        { protocol: "https", hostname: "orxstqqcsxatxaprqyvq.supabase.co", pathname: "/storage/v1/object/public/**" },
        { protocol: "https", hostname: "images.unsplash.com" },
        { protocol: "https", hostname: "http2.mlstatic.com" },
        { protocol: "https", hostname: "m.media-amazon.com" },
      ],
    },
  };
}
```

Supabase client (no changes — works once env vars are set in Vercel):
```typescript
// src/lib/supabase.ts
import { createClient } from "@supabase/supabase-js";
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

Package scripts (from package.json):
- `npm run dev`   -> `next dev --hostname 0.0.0.0`  (needs allowedDevOrigins for LAN phone testing)
- `npm run build` -> `next build`                   (production build; must NOT include allowedDevOrigins)
- `npm run start` -> `next start`
- `npm run lint`  -> `eslint`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor next.config.ts to a phase function (DEP-03, per D-01)</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (the file being modified — confirm current shape before editing)
    - .planning/phases/07-deploy/07-RESEARCH.md (Pattern 1 code block is the exact target)
    - .planning/phases/07-deploy/07-CONTEXT.md (D-01 locks this approach)
  </read_first>
  <behavior>
    - Expected: In production build (`npm run build`, which runs with phase !== PHASE_DEVELOPMENT_SERVER), the resolved config object does NOT contain an `allowedDevOrigins` key.
    - Expected: In dev (`npm run dev`, phase === PHASE_DEVELOPMENT_SERVER), the resolved config object DOES contain `allowedDevOrigins: ["192.168.100.14", "192.168.1.67", "192.168.1.74"]`.
    - Expected: `images.remotePatterns` is preserved verbatim (same 4 entries, same order, same fields) so Supabase storage, Unsplash, MercadoLibre, and Amazon images still load.
  </behavior>
  <action>
    Replace the entire contents of `next.config.ts` with the exact target shape below (per D-01 and RESEARCH.md Pattern 1). Do NOT invent any other changes.

    Concrete final content (copy verbatim):

    ```typescript
    import type { NextConfig } from "next";
    import { PHASE_DEVELOPMENT_SERVER } from "next/constants";

    export default function config(phase: string): NextConfig {
      return {
        ...(phase === PHASE_DEVELOPMENT_SERVER && {
          allowedDevOrigins: ["192.168.100.14", "192.168.1.67", "192.168.1.74"],
        }),
        images: {
          remotePatterns: [
            {
              protocol: "https",
              hostname: "orxstqqcsxatxaprqyvq.supabase.co",
              pathname: "/storage/v1/object/public/**",
            },
            {
              protocol: "https",
              hostname: "images.unsplash.com",
            },
            {
              protocol: "https",
              hostname: "http2.mlstatic.com",
            },
            {
              protocol: "https",
              hostname: "m.media-amazon.com",
            },
          ],
        },
      };
    }
    ```

    Reasoning / guardrails:
    - `PHASE_DEVELOPMENT_SERVER` is the string `"phase-development-server"` and only matches `next dev`. This guarantees the array is absent from `next build` output — satisfies DEP-03 (RESEARCH.md Pitfall 2 / Pattern 1).
    - Do NOT use `process.env.NODE_ENV === 'development'` instead — D-01 permits either, but the phase function is the canonical Next.js pattern and avoids env-var-misconfiguration edge cases.
    - Keep the three dev-origin IPs exactly as listed (user tests from those LAN IPs per STATE.md pattern `next dev --hostname 0.0.0.0`).
    - Do NOT remove or reorder any `remotePatterns` entry.
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run build 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PHASE_DEVELOPMENT_SERVER" next.config.ts` returns `2` (one import, one usage).
    - `grep -c "export default function config" next.config.ts` returns `1`.
    - `grep -c "const nextConfig" next.config.ts` returns `0` (the old static object export is gone).
    - `grep -c "allowedDevOrigins" next.config.ts` returns exactly `1` (only inside the spread, not at top level).
    - `grep -c "192.168.100.14" next.config.ts` returns `1` and the same for `192.168.1.67` and `192.168.1.74`.
    - `grep -c "orxstqqcsxatxaprqyvq.supabase.co" next.config.ts` returns `1`.
    - `grep -c "images.unsplash.com" next.config.ts` returns `1`.
    - `grep -c "http2.mlstatic.com" next.config.ts` returns `1`.
    - `grep -c "m.media-amazon.com" next.config.ts` returns `1`.
    - `npm run build` exits 0 and prints a successful build summary (look for `Compiled successfully` or `Creating an optimized production build ... ✓`).
    - `npm run build` output does NOT print any warning containing the string `allowedDevOrigins`.
  </acceptance_criteria>
  <done>
    next.config.ts is a phase function; `npm run build` succeeds with no config-related warnings; allowedDevOrigins only materializes in dev phase.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm LAN dev still works + lint passes</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (verify Task 1 wrote the expected content)
    - package.json (confirm dev/build/lint scripts unchanged)
  </read_first>
  <behavior>
    - Expected: `npm run lint` exits 0 with no errors on next.config.ts.
    - Expected: `npm run build` exits 0 (re-verified after Task 1 commit, in case formatter or editor altered the file).
    - Expected: No TypeScript type errors from the conditional spread pattern (RESEARCH.md Pitfall 2).
  </behavior>
  <action>
    Run lint and a fresh build as a final gate before deployment. Do not modify any files in this task. If either command fails, fix the issue in next.config.ts ONLY (do not touch other files), then re-run. Do NOT attempt to silence errors with `// @ts-ignore` or `eslint-disable` — the canonical pattern should type-check cleanly.

    Commands to run in order (concrete):
    1. `cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run lint`
    2. `cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run build`
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with no printed errors (warnings OK only if they existed before Phase 7 — do not add new ones).
    - `npm run build` exits 0.
    - Build output shows `Compiled successfully` or equivalent Next.js 16.2 success marker.
    - `.next/` directory exists after the build.
    - No occurrence of the string `Type error` or `Failed to compile` in combined output.
  </acceptance_criteria>
  <done>
    Lint clean, production build succeeds, config change is safe to push to main.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: MANUAL — Create Vercel project and deploy (DEP-01, DEP-02)</name>
  <files>(no files modified — external Vercel dashboard actions only)</files>
  <what-built>
    The code change for DEP-03 is committed on `main`. All that remains is creating the Vercel project, supplying the Supabase env vars, and triggering the first production deployment. These steps have no CLI path the user wants to take (per D-03 — dashboard import is the chosen approach).
  </what-built>
  <read_first>
    - .env.example (confirms the two env var names: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)
    - .planning/phases/07-deploy/07-RESEARCH.md — Pattern 2 and Pattern 3 (step-by-step Vercel flow)
    - User's local .env.local (source of the two values to paste into Vercel — NOT in git)
  </read_first>
  <action>
    **HUMAN ACTION REQUIRED — Claude cannot perform this task.** Per D-03, the user chose dashboard import (no CLI). Hand these exact instructions to the user and wait for the production URL in the resume-signal before proceeding to Task 4.

    Complete ALL of the following in order. Do not skip steps.

    1. **Ensure `main` is pushed to GitHub.** Run:
       - `cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && git push origin main`
       - Confirm the push includes the `next.config.ts` commit from Tasks 1–2.

    2. **Create or log in to Vercel account.** Go to https://vercel.com/signup (or /login if you already have one). Authorize Vercel to access the GitHub account that owns `computer-recomendator`.

    3. **Start the import flow.** Go to https://vercel.com/new, click "Import Git Repository", and select `computer-recomendator` from the list. If the repo does not appear, click "Adjust GitHub App Permissions" and grant access.

    4. **Verify framework auto-detection.** The import screen MUST show:
       - Framework Preset: `Next.js` (auto-detected — if it says anything else, stop and report)
       - Root Directory: `./` (default)
       - Build Command: default (leave blank)
       - Output Directory: default (leave blank)
       - Install Command: default (leave blank)

    5. **Add environment variables BEFORE clicking Deploy** (critical — RESEARCH.md Pitfall 1 — `NEXT_PUBLIC_` vars are inlined at build time).

       Copy exact values from local `.env.local` (or Supabase Dashboard → Project Settings → API) into Vercel's "Environment Variables" section:

       | Name                            | Value source                                                  | Scope                            |
       |---------------------------------|---------------------------------------------------------------|----------------------------------|
       | `NEXT_PUBLIC_SUPABASE_URL`      | From `.env.local` — starts with `https://`, ends `.supabase.co` | Production, Preview, Development |
       | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | From `.env.local` — long JWT starting with `eyJ`              | Production, Preview, Development |

       Per D-02, same Supabase project is used for all envs — check all three scope boxes.

    6. **Click "Deploy"** on the Vercel import screen. Wait for build (1–3 min). Watch log for:
       - `✓ Compiled successfully`
       - `✓ Collecting page data`
       - `✓ Generating static pages`
       - NO errors mentioning `supabaseUrl`, `undefined`, or `NEXT_PUBLIC_`.

    7. **Record the production URL.** After deploy finishes, copy the assigned URL (shape: `https://computer-recomendator-<hash>.vercel.app`). Paste into resume-signal below.

    If ANY step fails (build fails, env vars rejected, GitHub not connected), stop and report the exact error — do NOT proceed to Task 4.
  </action>
  <how-to-verify>
    See numbered steps 1–7 in `<action>` above — they are the verification procedure for this human-action checkpoint.
  </how-to-verify>
  <verify>
    Manual verification by human — Claude cannot programmatically inspect a Vercel dashboard. The executor MUST pause and wait for the resume-signal (production URL). After the URL is provided, Claude can run `curl -sI <PROD_URL> | head -1` to confirm HTTP 200 as a sanity check.
  </verify>
  <acceptance_criteria>
    - Vercel project exists in the user's Vercel dashboard, linked to the `computer-recomendator` GitHub repo, tracking the `main` branch.
    - Both `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` are present under Project → Settings → Environment Variables with Production scope checked.
    - Most recent deployment status in Vercel dashboard reads `Ready` (green).
    - Build log shows `Compiled successfully` and no errors.
    - `curl -sI <PROD_URL>` returns `HTTP/2 200` (or 307→200 redirect chain to the final URL).
    - User has pasted the production URL into the resume-signal.
  </acceptance_criteria>
  <done>
    A live `https://<project>.vercel.app` URL exists, responds with HTTP 200, and was built with the Supabase env vars present. The URL is recorded for Task 4.
  </done>
  <resume-signal>
    Paste the production URL (format: `https://<something>.vercel.app`) so Task 4 can smoke test it. If the deploy failed, paste the error instead and we will diagnose before retrying.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Post-deploy smoke test on production URL (verifies DEP-01, DEP-02)</name>
  <files>(no files modified — manual smoke test of the live URL)</files>
  <what-built>
    A Vercel production deployment of the app at the URL from Task 3. This task verifies that the public URL works without any local setup (DEP-01) and that Supabase data loads correctly in production (DEP-02). It also regression-tests DEP-03 (local dev still works).
  </what-built>
  <read_first>
    - .planning/phases/07-deploy/07-VALIDATION.md (Manual-Only Verifications table lists exactly these two checks)
    - .planning/ROADMAP.md Phase 7 Success Criteria (the three items being verified end-to-end)
  </read_first>
  <action>
    **HUMAN VERIFICATION REQUIRED.** Claude presents these instructions to the user; the user performs the checks and reports back. Claude does NOT attempt to automate browser testing.

    Open an **incognito / private browser window** (no cookies, no localStorage from dev testing) and perform all steps. Replace `<PROD_URL>` with the URL from Task 3.

    1. **DEP-01 — Public URL reachable without auth or setup:**
       - Visit `<PROD_URL>` in the incognito window.
       - Expected: Homepage renders fully — hero, navbar, footer visible. No error boundary. No 404 / 500 page.

    2. **DEP-02 — Supabase data loads (quiz flow):**
       - Navigate to `/quiz` (via navbar or direct URL).
       - Complete all 3 quiz steps by selecting any option in each carousel.
       - Expected: Quiz result renders with a profile name AND 5 laptop recommendations, each with real image, title, price, specs. Empty list or persistent skeletons = DEP-02 failing.

    3. **DEP-02 — Supabase data loads (catalog flow):**
       - Navigate to `/catalog`.
       - Expected: Catalog cards load with real laptop data (images, titles, prices). NOT empty, NOT stuck loading.

    4. **DEP-02 — Supabase data loads (comparator flow):**
       - Navigate to `/compare`.
       - Open the picker on one slot, search any laptop, pick one.
       - Expected: Laptop appears in the slot with all spec data populated.

    5. **DEP-03 sanity check:**
       - Open browser DevTools Console on `<PROD_URL>` and reload once.
       - Expected: NO console warnings mentioning `allowedDevOrigins` or cross-origin dev requests.

    6. **Regression — local dev still works (confirms D-01 preserved dev workflow):**
       - Terminal: `cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run dev`
       - From phone on same Wi-Fi, visit `http://<mac-LAN-IP>:3000` (one of 192.168.100.14 / 192.168.1.67 / 192.168.1.74).
       - Expected: App loads on phone. No cross-origin error.
       - Ctrl-C to stop dev server.

    If any step fails, stop and describe the exact failure (which URL, which step, what appeared, any console errors). Do NOT sign off.
  </action>
  <how-to-verify>
    See the six numbered steps in `<action>` above — they ARE the verification procedure.
  </how-to-verify>
  <verify>
    Manual verification by human — covers DEP-01 and DEP-02 per VALIDATION.md's Manual-Only Verifications table. Claude waits for `approved` in the resume-signal.
  </verify>
  <acceptance_criteria>
    - Step 1 passes: Homepage loads in incognito at `<PROD_URL>` with HTTP 200 and full UI.
    - Step 2 passes: Quiz completion yields a profile name AND 5 real laptops from Supabase (real images + titles + prices, not placeholders).
    - Step 3 passes: `/catalog` shows real laptop cards from Supabase.
    - Step 4 passes: `/compare` picker returns laptops when searched.
    - Step 5 passes: No `allowedDevOrigins` warnings in the production browser console.
    - Step 6 passes: `npm run dev` still accepts LAN-IP requests from phone — `allowedDevOrigins` still active in dev phase.
    - User has typed `approved` in the resume-signal.
  </acceptance_criteria>
  <done>
    All six steps pass; Phase 7 success criteria from ROADMAP.md are satisfied; user has signed off with `approved`.
  </done>
  <resume-signal>
    Type `approved` if all six steps passed. If any step failed, paste which step + exact error and we will diagnose.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase-level automated verification** (run before signing off):
- `cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run lint && npm run build` — exits 0.
- `grep -c "PHASE_DEVELOPMENT_SERVER" next.config.ts` returns `2`.
- `grep -c "export default function config" next.config.ts` returns `1`.
- `grep -c "const nextConfig" next.config.ts` returns `0`.
- `curl -sI <PROD_URL> | head -1` returns `HTTP/2 200` (or equivalent success).

**Phase-level manual verification:**
- Vercel dashboard shows the project with most-recent deployment status = `Ready`.
- Production URL loads the homepage, quiz returns real recommendations, catalog loads, comparator picker returns data.
- Local `npm run dev` still permits LAN-IP phone access.
</verification>

<success_criteria>
Phase 7 is complete when ALL of the following are true:

1. **DEP-01 (public URL):** A public Vercel `.vercel.app` URL reachable by any browser without auth, local setup, or VPN. Most-recent deployment = `Ready`.
2. **DEP-02 (Supabase in prod):** Completing the quiz, browsing `/catalog`, and using `/compare` on the production URL all return real data from the Supabase production project. No empty states that should have data.
3. **DEP-03 (no dev config in prod):** `next.config.ts` exports a phase function; `allowedDevOrigins` only appears inside a `PHASE_DEVELOPMENT_SERVER` conditional spread; `npm run build` completes with zero config-related warnings.
4. **Regression gate:** `npm run dev --hostname 0.0.0.0` still accepts requests from the three LAN IPs listed in D-01, preserving the dev-on-phone workflow.
5. **Commit state:** The `next.config.ts` change is committed on `main` and pushed to GitHub (which is what triggered the Vercel build in Task 3).
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy/07-01-SUMMARY.md` documenting:
- Final production URL (the `<PROD_URL>` from Task 3)
- Vercel project name/slug
- next.config.ts diff summary (before/after)
- Which env vars were set in Vercel and for which scopes
- Any deviations from the plan and why
- Any issues encountered during Vercel import or first build
</output>
