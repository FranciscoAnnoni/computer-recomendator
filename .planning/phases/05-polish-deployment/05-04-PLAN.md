---
phase: 05-polish-deployment
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - D-10
  - D-11
  - D-12
  - D-13
must_haves:
  truths:
    - "App layout is correct at 375px (iPhone SE)"
    - "App layout is correct at 430px (iPhone 14 Pro)"
    - "App layout is correct at 768px (iPad)"
    - "App layout is correct at 1280px (MacBook 13 inch)"
    - "App layout is correct at 1440px+ (wide desktop)"
    - "Navbar hamburger menu works at all mobile breakpoints"
    - "DetailOverlay is full-screen on mobile"
    - "Compare page shows 2 columns on mobile"
  artifacts: []
  key_links: []
---

<objective>
Validate responsive layout across all 5 target breakpoints from D-11 and fix any issues found inline. This is the final verification step before the phase is considered complete.

Purpose: Ensure the app looks correct and functions smoothly across mobile, tablet, and desktop. Per D-05, mobile is the priority. Per D-12, any issues found are fixed immediately as part of this plan.
Output: Verified responsive behavior, any inline fixes committed.
</objective>

<execution_context>
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-deployment/05-CONTEXT.md
@.planning/phases/05-polish-deployment/05-RESEARCH.md
@.planning/phases/05-polish-deployment/05-01-SUMMARY.md
@.planning/phases/05-polish-deployment/05-02-SUMMARY.md
@.planning/phases/05-polish-deployment/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and start dev server for responsive testing</name>
  <files></files>
  <read_first>
    - src/app/layout.tsx
    - src/components/layout/navbar.tsx
  </read_first>
  <action>
Run `npm run build` to confirm the full app compiles after Plans 01-03. Then start the dev server with `npm run dev`. The dev server is needed for the human verification step that follows.

If the build fails, diagnose and fix the issue before proceeding. Common causes after Plans 01-03:
- Missing import (Image, Metadata type)
- remotePatterns misconfiguration
- Type error in metadata export

Report the build status and dev server URL.
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no errors
    - Dev server is running and accessible
  </acceptance_criteria>
  <done>Build passes, dev server running for responsive testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive validation across 5 breakpoints</name>
  <files></files>
  <action>
Present the user with a structured responsive testing checklist. The user will test at 5 breakpoints (375px, 430px, 768px, 1280px, 1440px) using Chrome DevTools Device Toolbar. Any issues reported will be fixed inline per D-12.

What was built in Phase 5:
- Home hero staggered fade-up animation (Plan 02)
- DetailOverlay duration reduced to 0.25s (Plan 02)
- next/image migration in CatalogCard and CompareCard (Plan 01)
- SEO metadata on all 5 pages (Plan 03)
- Animation duration audit across codebase (Plan 02)

Testing instructions for the user:

Open http://localhost:3000 in Chrome. Use DevTools (Cmd+Option+I) then Device Toolbar to test at each breakpoint.

Test at each breakpoint (375px, 430px, 768px, 1280px, 1440px):

1. Home page — Hero heading, subtitle, CTA visible and centered. Fade-up animation plays on hard refresh (Cmd+Shift+R). No overflow.

2. Navbar — Hamburger menu opens/closes on mobile. Desktop nav links visible on 1280px+. No overlapping elements.

3. Quiz (/quiz) — Carousel cards fit within viewport width. Navigation arrows visible. No horizontal scroll.

4. Catalog (/catalog) — Cards render with images (verify next/image: right-click image then Inspect should show img inside span with srcset). Grid is single column. DetailOverlay slides up smoothly (should take about 250ms, feel snappy). Overlay covers full screen on mobile.

5. Compare (/compare) — Select 2 laptops. Cards show side by side (2 columns on mobile). Images render via next/image.

6. Profile (/profile, if quiz completed) — Content centered, no overflow.

7. Browser tabs — Each page shows unique title (not just "Computer Recomendator" on all).

Report any issues found. They will be fixed inline per D-12.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — user approval required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms layout is correct at all 5 breakpoints
    - Any reported issues are fixed and committed
  </acceptance_criteria>
  <done>User approves responsive layout at all 5 breakpoints, any inline fixes committed</done>
</task>

</tasks>

<verification>
1. Build passes after all Phase 5 changes
2. User confirms responsive layout at all 5 breakpoints
3. Any issues found during checkpoint are fixed inline
</verification>

<success_criteria>
- Build passes
- User approves responsive layout at 375px, 430px, 768px, 1280px, 1440px
- All focus areas from D-13 verified: navbar hamburger, quiz carousel, catalog grid, DetailOverlay full-screen mobile, compare columns
- Any inline fixes are committed
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deployment/05-04-SUMMARY.md`
</output>
