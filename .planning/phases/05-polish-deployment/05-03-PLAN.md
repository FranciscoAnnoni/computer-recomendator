---
phase: 05-polish-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - src/app/page.tsx
  - src/app/catalog/page.tsx
  - src/app/quiz/page.tsx
  - src/app/compare/page.tsx
  - src/app/profile/layout.tsx
autonomous: true
requirements:
  - D-14
  - D-15
  - D-16
  - D-17
must_haves:
  truths:
    - "Every public page has a unique title visible in the browser tab"
    - "Every public page has Open Graph title, description, and image meta tags"
    - "Quiz page OG tags are added without breaking existing metadata"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page metadata with OG tags"
      contains: "export const metadata"
    - path: "src/app/catalog/page.tsx"
      provides: "Catalog page metadata with OG tags"
      contains: "export const metadata"
    - path: "src/app/quiz/page.tsx"
      provides: "Quiz page metadata with OG tags added"
      contains: "openGraph"
    - path: "src/app/compare/page.tsx"
      provides: "Compare page metadata with OG tags"
      contains: "export const metadata"
    - path: "src/app/profile/layout.tsx"
      provides: "Profile route metadata via layout (page is use client)"
      contains: "export const metadata"
  key_links:
    - from: "src/app/page.tsx"
      to: "/public/og-image.png"
      via: "OG image reference in metadata"
      pattern: "og-image.png"
    - from: "src/app/layout.tsx"
      to: "all page metadata"
      via: "Next.js metadata merge — page metadata overrides layout metadata"
      pattern: "metadata"
---

<objective>
Add SEO metadata (title, description, Open Graph tags) to all 5 public pages using the Next.js Metadata API. This is the lowest-priority polish item per D-17 but completes the phase's scope.

Purpose: Each page gets a unique browser tab title and social sharing preview. When shared on social media, the app shows a proper title, description, and preview image instead of generic defaults.
Output: Metadata exports on all pages, OG image placeholder note.
</objective>

<execution_context>
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-deployment/05-CONTEXT.md
@.planning/phases/05-polish-deployment/05-RESEARCH.md

<interfaces>
<!-- Existing metadata pattern from quiz/page.tsx -->
From src/app/quiz/page.tsx:
```typescript
export const metadata = {
  title: "Quiz - Computer Recomendator",
  description: "Descubre tu laptop ideal en 3 pasos",
};
```

<!-- Layout base metadata that pages override -->
From src/app/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: "Computer Recomendator",
  description: "Find the perfect laptop for your needs",
  viewport: { width: "device-width", initialScale: 1, viewportFit: "cover" },
};
```

<!-- Profile page is "use client" — CANNOT export metadata from page.tsx -->
From src/app/profile/page.tsx:
```typescript
"use client";
// ... client component code
export default function ProfilePage() { ... }
```

<!-- Catalog and Compare pages are Server Components — can export metadata -->
From src/app/catalog/page.tsx:
```typescript
import { Suspense } from "react";
// No metadata export currently
export default function CatalogPage() { ... }
```

From src/app/compare/page.tsx:
```typescript
import { Suspense } from "react";
// No metadata export currently
export default function ComparePage() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metadata exports to home, catalog, quiz, and compare pages</name>
  <files>src/app/page.tsx, src/app/catalog/page.tsx, src/app/quiz/page.tsx, src/app/compare/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/app/catalog/page.tsx
    - src/app/quiz/page.tsx
    - src/app/compare/page.tsx
    - src/app/layout.tsx
  </read_first>
  <action>
Add `export const metadata: Metadata` to 4 Server Component pages. For quiz/page.tsx, add the `openGraph` block to the existing metadata export. Per D-14, each page needs title, description, and OG tags. Per D-15, reference `/og-image.png` as the OG image (file may not exist yet — that's fine, metadata is correct regardless).

**1. src/app/page.tsx** — Add above `export default function Home()`:

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Computer Recomendator",
  description: "Encuentra la laptop perfecta para tus necesidades con recomendaciones de expertos.",
  openGraph: {
    title: "Computer Recomendator",
    description: "Encuentra la laptop perfecta para tus necesidades con recomendaciones de expertos.",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};
```

Note: page.tsx was modified by Plan 02 to import HeroSection. The metadata export is added alongside those existing imports. Keep the Container and HeroSection imports from Plan 02.

**2. src/app/catalog/page.tsx** — Add above `export default function CatalogPage()`:

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Catalogo | Computer Recomendator",
  description: "Explora laptops curadas con recomendaciones de expertos.",
  openGraph: {
    title: "Catalogo | Computer Recomendator",
    description: "Explora laptops curadas con recomendaciones de expertos.",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};
```

**3. src/app/quiz/page.tsx** — Update existing metadata to add `Metadata` type and `openGraph`:

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Quiz - Computer Recomendator",
  description: "Descubre tu laptop ideal en 3 pasos",
  openGraph: {
    title: "Quiz - Computer Recomendator",
    description: "Descubre tu laptop ideal en 3 pasos",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};
```

**4. src/app/compare/page.tsx** — Add above `export default function ComparePage()`:

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comparar | Computer Recomendator",
  description: "Compara laptops lado a lado para encontrar tu mejor opcion.",
  openGraph: {
    title: "Comparar | Computer Recomendator",
    description: "Compara laptops lado a lado para encontrar tu mejor opcion.",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};
```

Do NOT add accented characters in metadata (avoid encoding issues) — use plain text: "Catalogo" not "Catalogo", "opcion" not "opcion".

Per D-16, do NOT add sitemap, robots.txt, or structured data.
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && grep -l "export const metadata" src/app/page.tsx src/app/catalog/page.tsx src/app/quiz/page.tsx src/app/compare/page.tsx | wc -l | tr -d ' ' && grep -c "openGraph" src/app/quiz/page.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `export const metadata: Metadata`
    - src/app/catalog/page.tsx contains `export const metadata: Metadata`
    - src/app/quiz/page.tsx contains `openGraph` block (added to existing metadata)
    - src/app/compare/page.tsx contains `export const metadata: Metadata`
    - All 4 files contain `og-image.png` reference
    - All 4 files import `type { Metadata } from "next"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>4 Server Component pages have metadata with title, description, and OG tags. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile route metadata via layout.tsx</name>
  <files>src/app/profile/layout.tsx</files>
  <read_first>
    - src/app/profile/page.tsx
    - src/app/layout.tsx
  </read_first>
  <action>
The profile page (`src/app/profile/page.tsx`) is a `"use client"` component and CANNOT export `metadata` (Next.js ignores metadata exports from Client Components). The solution is to create a route-level `layout.tsx` for the profile directory that exports the metadata.

**Create `src/app/profile/layout.tsx`:**

```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Mi Perfil | Computer Recomendator",
  description: "Tu perfil de uso y laptops recomendadas.",
  openGraph: {
    title: "Mi Perfil | Computer Recomendator",
    description: "Tu perfil de uso y laptops recomendadas.",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

This layout is a pass-through — it renders children unchanged. Its only purpose is to provide metadata for the profile route. Next.js merges this with the root layout metadata, with this layout's values taking priority.

Do NOT modify `src/app/profile/page.tsx` — it remains a `"use client"` component unchanged.
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && test -f src/app/profile/layout.tsx && grep -q "export const metadata" src/app/profile/layout.tsx && grep -q "openGraph" src/app/profile/layout.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/app/profile/layout.tsx exists
    - profile/layout.tsx contains `export const metadata: Metadata`
    - profile/layout.tsx contains `openGraph` with `og-image.png`
    - profile/layout.tsx contains `title: "Mi Perfil | Computer Recomendator"`
    - profile/layout.tsx exports a default function that renders children
    - src/app/profile/page.tsx is NOT modified
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Profile route has metadata via layout.tsx, all 5 public pages now have SEO metadata. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `grep -rl "export const metadata" src/app/` returns 6 files (layout.tsx + 4 pages + profile/layout.tsx)
3. `grep -rl "openGraph" src/app/` returns 5 files (all pages including quiz)
4. All metadata contains `og-image.png` reference
5. Profile page.tsx is unchanged (still "use client")
</verification>

<success_criteria>
- All 5 public pages (home, catalog, quiz, compare, profile) have unique titles and OG metadata
- Quiz page's existing metadata is extended with OG tags, not replaced
- Profile metadata is served via route layout.tsx (workaround for "use client" page)
- No page lost its existing functionality
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deployment/05-03-SUMMARY.md`
</output>
