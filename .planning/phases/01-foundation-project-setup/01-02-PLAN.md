---
phase: 01-foundation-project-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/navbar.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/container.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - RNF1.1
  - RNF1.2
  - RNF3.1

must_haves:
  truths:
    - "Every page displays a sticky navbar with logo and navigation links"
    - "Navbar has frosted glass effect on scroll"
    - "Mobile users see a hamburger menu that opens a slide-in sheet"
    - "Every page displays a minimal footer with copyright"
    - "Content is centered in a max-width container with responsive padding"
  artifacts:
    - path: "src/components/layout/navbar.tsx"
      provides: "Sticky navigation with frosted glass and mobile hamburger"
      min_lines: 40
    - path: "src/components/layout/footer.tsx"
      provides: "Minimal copyright footer"
      min_lines: 5
    - path: "src/components/layout/container.tsx"
      provides: "Centered max-width content wrapper"
      min_lines: 8
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/navbar.tsx"
      via: "import and render in root layout"
      pattern: "import.*Navbar.*from.*layout/navbar"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "import and render in root layout"
      pattern: "import.*Footer.*from.*layout/footer"
    - from: "src/components/layout/navbar.tsx"
      to: "src/components/ui/button.tsx"
      via: "shadcn Button for CTA"
      pattern: "import.*Button.*from.*ui/button"
---

<objective>
Build the layout shell: Navbar (sticky, frosted glass, mobile hamburger), Footer (minimal copyright), and Container (centered max-width wrapper). Wire them into the root layout so every page inherits the structure.

Purpose: Establish the visual frame that all pages share, setting the Apple-minimalist tone from the first render.
Output: Three layout components wired into layout.tsx, responsive from mobile to desktop.
</objective>

<execution_context>
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FranciscoAnnoni/Proyectos/computer-recomendator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-project-setup/01-CONTEXT.md
@.planning/phases/01-foundation-project-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Container and Footer components</name>
  <files>src/components/layout/container.tsx, src/components/layout/footer.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-project-setup/01-CONTEXT.md (footer spec: copyright only)
    - src/app/globals.css (available CSS custom properties / design tokens)
    - tailwind.config.ts (available typography and color tokens)
  </read_first>
  <action>
    1. Create `src/components/layout/container.tsx`:
       - A simple wrapper component that accepts `children` and optional `className`
       - Default classes: `mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8`
       - Use `cn()` from `@/lib/utils` to merge custom className with defaults
       - Export as named export `Container`

    2. Create `src/components/layout/footer.tsx`:
       - Renders a `<footer>` element with `border-t border-border py-8`
       - Contains a Container wrapping: `<p className="text-small text-muted-foreground text-center">© 2026 Computer Recomendator</p>`
       - Export as named export `Footer`
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/container.tsx contains "export function Container" or "export const Container"
    - src/components/layout/container.tsx contains "max-w-7xl"
    - src/components/layout/container.tsx contains "cn(" (uses utility for className merging)
    - src/components/layout/footer.tsx contains "export function Footer" or "export const Footer"
    - src/components/layout/footer.tsx contains "2026 Computer Recomendator"
    - src/components/layout/footer.tsx contains "text-small"
    - src/components/layout/footer.tsx contains "border-t"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Container provides centered max-width wrapper. Footer shows copyright text with muted styling and top border.</done>
</task>

<task type="auto">
  <name>Task 2: Create Navbar with frosted glass and mobile menu, wire layout</name>
  <files>src/components/layout/navbar.tsx, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-project-setup/01-CONTEXT.md (navbar spec: logo left, links right, frosted glass, mobile hamburger sheet)
    - src/app/layout.tsx (current state after Plan 01)
    - src/components/ui/button.tsx (shadcn Button API)
    - src/components/layout/container.tsx (just created, for use in navbar)
    - src/components/layout/footer.tsx (just created, for use in layout)
  </read_first>
  <action>
    1. Add shadcn Sheet component for mobile nav:
       `npx shadcn@latest add sheet`

    2. Create `src/components/layout/navbar.tsx` as a client component ("use client"):

       **Desktop layout (hidden on mobile, visible md+):**
       - `<nav>` with sticky positioning: `sticky top-0 z-50`
       - Frosted glass: `backdrop-blur-md bg-background/80 border-b border-border`
       - Inside Container: flex row, justify-between, items-center, h-16
       - Left: Logo text "Computer Recomendator" as Link to "/" with `font-semibold text-foreground`
       - Right: nav links group
         - Link "Catalog" to "/catalog" — `text-body text-muted-foreground hover:text-foreground transition-colors`
         - Link "Compare" to "/compare" — same styling
         - shadcn Button (default variant) as Link to "/quiz" — text "Find My Laptop" with right arrow entity
       - Use Next.js `Link` component from "next/link" for all navigation

       **Mobile layout (visible on small screens, hidden md+):**
       - Same sticky nav bar but right side has a hamburger button (Menu icon from lucide-react)
       - Hamburger triggers shadcn Sheet (side="right")
       - Sheet content: vertical stack of same nav links + CTA button
       - Sheet closes on link click (use SheetClose or state management)

       **Scroll detection (optional enhancement, Claude's discretion):**
       - Use useState + useEffect with scroll listener
       - Add/remove a subtle shadow class on scroll (e.g., `shadow-sm` when scrollY > 0)

    3. Update `src/app/layout.tsx`:
       - Import Navbar and Footer
       - Structure body content as:
         ```
         <Navbar />
         <main className="min-h-screen">{children}</main>
         <Footer />
         ```

    4. Update `src/app/page.tsx`:
       - Wrap content in Container
       - Keep the heading/subtitle/button from Plan 01 but ensure they look good within the layout frame
       - Add some vertical padding: `py-20` or similar for the hero section
  </action>
  <verify>
    <automated>cd /Users/FranciscoAnnoni/Proyectos/computer-recomendator && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/navbar.tsx contains "use client"
    - src/components/layout/navbar.tsx contains "backdrop-blur"
    - src/components/layout/navbar.tsx contains "sticky top-0"
    - src/components/layout/navbar.tsx contains "Find My Laptop"
    - src/components/layout/navbar.tsx contains 'Link' import from "next/link"
    - src/components/layout/navbar.tsx contains "Sheet" import from shadcn
    - src/components/layout/navbar.tsx contains "/quiz" (CTA link target)
    - src/components/layout/navbar.tsx contains "/catalog" (nav link)
    - src/components/layout/navbar.tsx contains "/compare" (nav link)
    - src/app/layout.tsx contains "Navbar" import
    - src/app/layout.tsx contains "Footer" import
    - src/app/page.tsx contains "Container" import
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Navbar renders with frosted glass effect, desktop nav links (Catalog, Compare, Find My Laptop CTA), mobile hamburger with slide-in sheet. Footer and Navbar are wired into root layout. Landing page uses Container for centered content.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Dev server shows: sticky navbar with logo, nav links, and blue CTA button
- Resizing to mobile width shows hamburger menu instead of nav links
- Clicking hamburger opens a slide-in sheet with nav links
- Footer shows "2026 Computer Recomendator" at the bottom
- Content is horizontally centered with responsive padding
</verification>

<success_criteria>
- Navbar: sticky, frosted glass, logo left, links right (Catalog, Compare, Find My Laptop CTA)
- Navbar mobile: hamburger icon opens shadcn Sheet with nav links
- Footer: minimal copyright line
- Container: max-w-7xl centered wrapper with responsive horizontal padding
- All three components wired into root layout
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-setup/01-02-SUMMARY.md`
</output>
