---
phase: 06-iterative-improvements
task: ad-hoc catalog audit
status: paused
last_updated: 2026-04-08T09:09:49.232Z
---

<current_state>
Ad-hoc ML catalog audit and cleanup. All JSON files updated. DB migration applied. Supabase import 12/130 rows done — batches 1-6 still pending due to context exhaustion.
</current_state>

<completed_work>

- ✅ Full ML API audit: 138 laptops checked via `/products/{id}/items` + `/users/{seller_id}`
- ✅ 8 laptops DELETED from JSONs (BAJO_REP sellers or SIN_STOCK)
- ✅ 15 affiliate links updated to better catalog alternatives
- ✅ 28 laptops marked `availability_warning: true` (no Platinum/Gold seller found)
- ✅ DB migration applied: `ALTER TABLE laptops ADD COLUMN IF NOT EXISTS availability_warning BOOLEAN DEFAULT FALSE`
- ✅ `scripts/ml_import_db.py` updated to include `availability_warning` in `to_db_row()`
- ✅ `supabase/migrations/05_add_availability_warning.sql` created
- ⏳ Supabase import: batch 0 done (12 rows), batches 1-6 PENDING
</completed_work>

<remaining_work>

1. **IMMEDIATE** — Complete Supabase import (batches 1-6):
   - Files: `/tmp/batch_001.sql` through `/tmp/batch_006.sql` (ephemeral — may be gone)
   - If files missing, regenerate: run the SQL generation Python script (see HANDOFF.json context_notes)
   - Use `mcp__supabase__execute_sql` with project_id=`orxstqqcsxatxaprqyvq`
   - Verify: `SELECT COUNT(*), COUNT(*) FILTER (WHERE availability_warning=TRUE) FROM laptops;` → expect 130 total, 28 red

2. **Frontend red label** — Add badge/indicator for `availability_warning=true` on laptop cards

3. **Profile-to-laptop assignment** — 81 profiles × 5 laptops each (next major task)
</remaining_work>

<decisions_made>

- `availability_warning BOOLEAN` field added to DB (not a tag) — cleaner for querying/UI
- Red-labeled laptops KEPT in catalog (user decision) — shown with visual warning
- `ml_data_active/*.json` is source of truth — `seed.sql` is legacy, not updated
- ML API: `/products/{id}/items` endpoint works with client_credentials token
- Supabase anon key blocks INSERT via REST — must use MCP `execute_sql` or service role key
</decisions_made>

<blockers>

- Supabase anon key → 401 on INSERT. **Workaround**: use `mcp__supabase__execute_sql` directly
- `/tmp/batch_*.sql` files are ephemeral — regenerate if lost (see HANDOFF.json)
</blockers>

<context>
User wants clean laptop catalog with verified sellers before building profile-to-laptop assignments. After completing the import, the next task is assigning 5 laptops per profile (81 profiles total) — user said they'll help validate the assignments and write the recommendation text per laptop.
</context>

<next_action>
1. Run SQL batches 1-6 via mcp__supabase__execute_sql (project_id=orxstqqcsxatxaprqyvq)
2. Verify count: SELECT COUNT(*), COUNT(*) FILTER (WHERE availability_warning=TRUE) FROM laptops;
3. Commit ml_data_active/ changes + migration file
4. Then: profile-to-laptop assignment task
</next_action>
