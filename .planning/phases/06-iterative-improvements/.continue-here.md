---
phase: 06-iterative-improvements
task: ad-hoc store scraping
status: paused
last_updated: 2026-04-08T10:20:21.491Z
---

<current_state>
Scraped 69 new laptops from ML stores (PC Center, TuNotebook, EXO, Apple API, Gfast search).
Output at /tmp/new_laptops_scraped.json — EPHEMERAL.
DB confirmed at 122 laptops / 23 red-labeled (previous import was complete, old HANDOFF was stale).
Profiles table: 81 profiles, 0 with laptop_ids assigned.
Waiting for user to review JSON before importing.
</current_state>

<completed_work>

- ✅ Previous session: Full ML catalog audit, 8 deleted, 15 links fixed, 28 red-labeled, migration applied, all 122 rows imported to DB
- ✅ This session: Verified DB state (122 laptops, all brands present — import was complete)
- ✅ Built scraper script: `scripts/ml_scrape_new.py` (HTML tienda scrape + API fallback)
- ✅ Scraped 5 specific products requested by user (Lenovo IdeaPad Slim3 Ryzen7, HP 255 G10 x2, Gfast N-758R, N-536R)
- ✅ Scraped PC Center Computers tienda: 29 laptops (Lenovo, HP, Gfast, Dell)
- ✅ Scraped TuNotebook TechStore: 27 laptops (HP, Asus, Acer, MSI, Dell, Lenovo)
- ✅ Scraped EXO tienda: 5 EXO laptops
- ✅ Apple via products/search API: 3 MacBook Air M3 (tienda not scrapeable)
- ✅ Gfast brand search: 14 models total
- ✅ Dedup + filter → 69 laptops saved to /tmp/new_laptops_scraped.json
</completed_work>

<remaining_work>

1. **User reviews** `/tmp/new_laptops_scraped.json` (69 laptops with affiliate links)
   - If file missing (reboot): run `python3 scripts/ml_scrape_new.py` (~3 min)
   - Preview: `python3 -c "import json; [print(l['brand'], int(l['price']/1000), 'k  ', l['name'][:55]) for l in json.load(open('/tmp/new_laptops_scraped.json'))['laptops_flat']]"`

2. **Import approved laptops** to Supabase via `mcp__supabase__execute_sql` (project_id=orxstqqcsxatxaprqyvq)
   - Build INSERT SQL from the approved JSON entries
   - Use ON CONFLICT DO NOTHING to avoid duplicate errors

3. **Fix OS bug in seed.sql** before running profile DO block:
   - Line ~303: change `'macOS Sequoia'` → `'macOS'` (DB stores 'macOS', not 'macOS Sequoia')
   - Then run the DO block (seed.sql lines 269-338) via mcp__supabase__execute_sql

4. **Profile-to-laptop assignment**: Run DO block → assigns 5 laptops to each of 81 profiles
   - Verify: `SELECT COUNT(*) FROM profiles WHERE array_length(laptop_ids, 1) = 5;` → should be 81

5. **Frontend**: Add red badge for `availability_warning=true` laptops on CatalogCard component
</remaining_work>

<decisions_made>

- `www.mercadolibre.com.ar/tienda/{slug}?category_id=MLA1652` works for HTML scraping; `listado.mercadolibre.com.ar` redirects to account verification
- Only 7-9 digit MLA IDs from HTML are catalog product IDs accessible via `/products/{id}` API
- `/items/{id}` and `/users/{id}/items/search` blocked (403) with client_credentials
- Apple tienda uses JS rendering → 0 catalog IDs in HTML → used products/search API fallback
- Pygcomexarg is a bare tienda → products covered by PC Center + Gfast search
- `ml_data_active/*.json` is source of truth; `seed.sql` is legacy (not used for imports)
- DB has 'macOS' not 'macOS Sequoia' → profile DO block needs this fix before running
</decisions_made>

<blockers>

- Apple official store products inaccessible via API (client_credentials). Only 3 MacBook Air M3 scraped (third-party resellers). DB already has 10 Apple laptops from previous session — acceptable.
- Pygcomexarg not scrapeable — products covered elsewhere.
</blockers>

<context>
User is building out the laptop catalog incrementally. They want to add new laptops from specific ML stores, review them, then replace weak entries. The profile DO block is critical — without it the recommendation engine returns nothing for each user profile. After that, the availability_warning red badge is the last pending UI feature from the catalog audit.
</context>

<next_action>
1. If /tmp/new_laptops_scraped.json missing: `python3 scripts/ml_scrape_new.py`
2. User reviews the 69 laptops
3. Build INSERT SQL for approved laptops → run via mcp__supabase__execute_sql
4. Fix seed.sql ~line 303: 'macOS Sequoia' → 'macOS'
5. Run DO block (seed.sql:269) via mcp__supabase__execute_sql (project_id=orxstqqcsxatxaprqyvq)
6. Verify: SELECT COUNT(*) FROM profiles WHERE array_length(laptop_ids, 1) = 5; → expect 81
</next_action>
